<div id="mySidebar" class="SideBar sidebar@(IsShown ? "" : " hidden")">
    <a href="#">About</a>
    <a href="#">Services</a>
    <a href="#">Clients</a>
    <a href="#">Contact</a>
</div>

@code {
    [Parameter]
    public bool IsShown { get; set; }

    [Parameter]
    public int Width { get; set; }
}

<style>
    .sidebar {
        background-color: teansparent;
        ;
        min-width: 50px;
        max-width: @($"{Width}px");
        transition: var(--transition-props);
        height: 100%;
        height: 100vh;
    }

        .sidebar.hidden {
            margin-left: -110px;
            transition: var(--transition-props);
        }


        /* The SideBar links */
        .SideBar a {
            padding: 8px 0px 0px 8px;
            text-decoration: none;
            font-size: 25px;
            color: #000000;
            display: flex;
            transition: 0.3s;
        }

            /* When you mouse over the navigation links, change their color */
            .SideBar a:hover {
                color: #C11AC1;
            }
</style>